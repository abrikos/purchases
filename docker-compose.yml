services:
  nginx:
    image: nginx:latest
    ports:
      - "127.0.0.1:8000:80"
    volumes:
      - ./docker_volumes/nginx:/etc/nginx/conf.d
    #restart: always
    depends_on:
      - backend

  backend:
    env_file:
      - ./.env
    environment:
      DB_HOST: db
    build: .
    command: >
      bash -c "./start-django-in-docker.sh"
    volumes:
      - front_bundles:/app/docker_volumes/static
    expose:
      - "8000"
    depends_on:
       - db
       - redis
    develop:
      watch:
        - action: sync
          path: .
          target: /app
          ignore:
            - .git/
            - .github/
            - static/
            - migrate.sh
            - freeze.sh

  db:
    image: postgres:16.0
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    expose:
      - "5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

networks:
  default:

volumes:
  redis_data:
  postgres_data:
    driver: local
  front_bundles:
    driver: local
